// import "../stylesheets/PaginationBtns.css";

export default function PaginationBtns({ pages, currentPage, onSelect }) {
  function changePage({ selected }) {
    console.log({ selected });
  }

  return (
    <div>PAGINATION BUTTONS</div>
  )
}
